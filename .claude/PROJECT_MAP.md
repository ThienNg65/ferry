# Ferry — Project Map

## What this is

Ferry is a lightweight WinSCP alternative: an Electron + Vue 3 desktop app for SFTP file transfer with a dual-pane browser, a transfer queue (recursive — whole folder trees, not just single files, with per-row retry on failure), and — its main differentiators — a genuinely live remote log tail (`tail -F` over SSH), a remote "Extract Here" archive-extraction action (zip and tar variants), browser-like site tabs (multiple concurrent SSH sessions, switchable instantly, deduped so re-connecting to an already-open site just switches to its tab), and an interactive SSH Terminal per site (auto-connects in the background as soon as the site connects, labeled `username@host`, opens directly at the site's configured remote start path if one is set), all wrapped in a minimal Apple-HIG-inspired UI built on `@nuxt/ui` v4. The file panes support click-to-sort columns, Explorer-style multi-select (Ctrl/Shift/Ctrl+A), a right-click context menu, F2 rename, editable permissions (chmod), and OS-level drag-and-drop (drop files/folders in from Explorer to upload; drag local rows out to Explorer via a native OS drag). Auth supports password, private key, SSH agent, and connecting through a jump/bastion host; every connection is host-key-verified (trust-on-first-use, warns on change) and real keyboard-interactive/2FA challenges are forwarded to the user instead of being guessed at. SFTP/SSH only (no FTP/FTPS/SCP). Single-window, no `vue-router` (removed round-5 — was an unused dependency) — view (and tab) switching is plain reactive state.

## Tech stack

- Electron 30.5.1, electron-vite 2.x (build tool), electron-builder 24.x (packaging, NSIS)
- Vue 3.5 (Composition API, `<script setup>`), Pinia 2.1, TypeScript 5.4 (strict)
- `@nuxt/ui` 4.9.0 (installed from npm, NOT from the local source clone at `C:\toys\ui`) + Tailwind CSS v4 (`@tailwindcss/vite`)
- `ssh2` 1.16 (direct — not `ssh2-sftp-client`, since raw exec channels are needed for tail/unzip alongside SFTP, and `client.shell()` for the interactive Terminal's PTY)
- `@xterm/xterm` 5.5 + `@xterm/addon-fit` 0.10 (renderer-side terminal emulator for the Terminal dock tab)
- `electron-store` (saved sites, plain JSON) + Electron's `safeStorage` (DPAPI-backed credential encryption — no `keytar`)
- `@iconify/vue` + `@iconify-json/lucide` (full icon set pre-registered at startup — see Gotchas)
- Node v22.14.0. npm installs on this machine need `--registry=https://registry.npmjs.org` explicitly.

## Directory structure

```
src/
  main/           Electron main process (Node context)
    ssh/          SessionManager (connection pool — host-key verification, real keyboard-interactive/2FA, SSH-agent + jump-host tunneling all live here, see Conventions), RemoteShell (exec/execLines/sftp wrapper, incl. chmod), KnownHostsStore (trust-on-first-use host-key pinning, `known_hosts.json`), keyboardInteractive (pure prompt-partitioning helpers), errors, retry, shellEscape (single-quote POSIX shell escaping, shared by UnzipService and TerminalManager — see Conventions)
    fs/           LocalFsService, RemoteFsService — thin functional wrappers, not classes
    transfer/     TransferQueue — bounded-concurrency upload/download over SFTP streams, single-file or a whole recursive directory tree (one queue slot each — see Conventions); failed/cancelled jobs are retried by the renderer re-calling enqueue from scratch, not by anything in this class
    tail/         TailManager — tail -F multiplexer with PID-capture kill + reconnect + idle reaper
    terminal/     TerminalManager — one interactive SSH shell (PTY, via RemoteShell.openShell) per terminalId, mirrors TailManager's shape but with no auto-reconnect; injects a `cd <remoteInitialPath>` on open if the site has one configured — see Conventions
    unzip/        UnzipService — remote archive extraction via SSH exec (`unzip` for .zip, `tar` for .tar/.tar.gz/.tgz/.tar.bz2/.tbz2 — see shared/archive.ts)
    sites/        SiteStore — saved connections (electron-store) + safeStorage-encrypted secrets, including a saved site's optional jump-host hop (its own secrets encrypted the same way) and agent-path override
    ipc/          One *.ipc.ts per domain, each exposing `registerXHandlers()`; envelope.ts has the typed `handle()` wrapper
    index.ts      App bootstrap: BrowserWindow (hardened), registers all IPC handlers
  preload/        contextBridge whitelist (invoke/on/off), derived from shared/contract.ts — the ONLY renderer↔main bridge
  shared/
    contract.ts   Single source of truth: INVOKE_CHANNELS, EVENT_CHANNELS, every payload type. Both preload and main import from here.
    archive.ts    Pure `archiveKind()`/`isArchive()` extension-recognition helpers (no Electron/Node deps) — imported by both the renderer (fileTypes.ts, gating "Extract here") and main (UnzipService.ts, choosing the extraction command)
  renderer/
    index.html, src/main.ts (mounts app, then defers full Lucide icon-set registration off the cold-start path — see Conventions)
    src/App.vue                    Root: TitleBar + SiteTabBar + (SessionManagerView | dual FilePane + BottomDock, lazy-loaded — see Conventions) for the active tab, hosts <UApp :toaster>
    src/components/
      shell/      TitleBar (fully custom minimize/maximize/close buttons, centered title, global busy indicator — see Conventions), SiteTabBar (browser-like open-site tab chips: label/spinner/error, close, +new, and the hide-Local-pane toggle pinned to the right — see Conventions), BottomDock (tab shell for Transfers/Log Tail/Terminal, animated collapse)
      sessions/   SessionManagerView (saved-sites list + quick-connect form + app version footer — always renders for whichever tab is in "picker" state; also hosts the host-key-mismatch warning dialog and the keyboard-interactive/2FA prompt dialog, both connect-time-only so they belong here — see Conventions), SiteFormDialog (UModal, add/edit a saved site, incl. agent-path override and the jump-host sub-form)
      files/      FilePane (owns per-pane keyboard shortcuts — F2 rename, Delete, Ctrl+R, Ctrl+A, animated Local-pane collapse, OS-vs-in-app drag detection — see Conventions), FileList (sortable column-header row + row list), FileRow (remote-only permissions cell + fixed-width action-icon slots + right-click `ContextMenu` + inline rename input — see Conventions), FileToolbar (also hosts the "Upload/Download N selected" bulk-transfer button), PathBreadcrumb, FilePreviewDialog (UModal, double-click file preview + tail shortcut), ChmodDialog (UModal, WinSCP-style owner/group/other read/write/execute checkbox grid, reachable from the permissions column's right-click menu)
      terminal/   TerminalView — thin host that attaches/shows per-session xterm.js instances live in terminalStreams.store.ts (see Conventions); never owns a Terminal itself
      transfers/  TransferQueue, TransferItem (gained a retry button for error/cancelled rows)
      logs/       LogTailViewer
    src/stores/     One Pinia store per domain: sessions (SessionTab[] — browser-like open-site tabs, all connected concurrently, tab-deduped per site; also holds the global pending host-key-mismatch/keyboard-interactive-prompt state — see Conventions), sites, localFs (global, session-agnostic), remoteFs (per-session keyed — see Conventions), transferQueue (enqueue/cancel/retry), tailStreams, terminalStreams (per-session xterm.js instance cache, `@xterm/xterm`/`@xterm/addon-fit` lazy-imported on first use — see Conventions), ui (layout/display prefs — hide-local-pane, permissions display mode — persisted to localStorage), ipcActivity (generic in-flight IPC-call counter driving the title-bar busy indicator — see Conventions)
    src/composables/ useLogTail (rAF-batched line buffering), useDragAndDrop (module-scoped shared drag payload), useNotify (thin wrapper over @nuxt/ui's useToast — see Conventions), useGlobalActivity (aggregates connecting/transfer/loading state across stores into one busy flag — see Conventions)
    src/utils/      fileTypes.ts — text-preview/log-file extension allowlists plus a re-export of shared/archive.ts's archive helpers; permissions.ts — parses the remote-only octal `FileEntry.permissions` string into technical (`rwxr-xr-x`) and friendly-label formats, plus `parseMode`/`formatMode` (pure octal-string <-> editable owner/group/other-triplet conversion, used by ChmodDialog); fileSort.ts — pure `compareEntries(column, direction)` comparator shared by both fs stores (dirs always sort before files); fileSelection.ts — pure `selectOnly`/`toggleSelect`/`selectRange`/`selectAll` functions implementing Explorer-style click/Ctrl/Shift/Ctrl+A selection, shared by both fs stores
    src/api.ts      invoke()/onEvent() wrappers — unwraps the IpcResult envelope, JSON-round-trips args to strip Vue proxies; also exports `IpcError` (thrown by `invoke()` on `{ok:false}`), which carries the stable `IpcErrorCode` (not just the message) so a caller can branch on failure kind (e.g. `HOST_KEY_MISMATCH`) without parsing text
.claude/
  architecture/   Full design docs (backend + frontend) written before implementation
  plan/           Approved implementation plans (ferry-implementation-plan.md = MVP, ferry-feature-round2-plan.md = round-2 UX features, ferry-feature-round3-plan.md = site tabs/Terminal/named sessions/collapsible Local rail, ferry-feature-round4-plan.md = titlebar/Activity-removal/tab-dedup/auto-terminal/permissions-column/scrollbar/tooltips/Ctrl+R/app-icon, ferry-feature-round5-plan.md = startup perf/extensions+archives/dialog size/busy indicator/animation/version/pane labels/terminal start-path)
resources/        App icon/favicon source assets (`1024x1024.png` is electron-builder's icon source, `favicon.ico` is the dev-mode BrowserWindow icon — see Gotchas)
```

## Entry points & key config

- `src/main/index.ts` — main process bootstrap, `createWindow()`, `registerAllHandlers()`
- `src/renderer/src/main.ts` — renderer bootstrap; **registers the full Lucide icon set here** (`addCollection`)
- `src/shared/contract.ts` — change this FIRST when adding any new IPC channel; preload/main won't compile otherwise
- `electron.vite.config.ts` — main/preload/renderer build config, path aliases (`@`, `@renderer`, `@shared`)
- `electron-builder.yml` — packaging config. **Must be named exactly this** (see Gotchas)
- `tsconfig.node.json` / `tsconfig.web.json` — split TS project references (main+preload+shared vs. renderer+shared)

## How the pieces talk to each other

- Renderer → main: `window.api.invoke(channel, ...args)` → preload validates channel against `INVOKE_CHANNELS`/`EVENT_CHANNELS` whitelist → `ipcMain.handle` (registered via `handle()` in `ipc/envelope.ts`) → always resolves `{ok:true,data}` or `{ok:false,code,message}`. Renderer's `api.ts` `invoke()` unwraps this and throws on `ok:false`.
- Main → renderer: `webContents.send(EVENT_CHANNELS.x, payload)` broadcast to all windows; renderer subscribes via `window.api.on(channel, cb)` (`onEvent()` wrapper).
- All SSH operations for a connection go through one `RemoteShell` instance (wraps a single `ssh2.Client`), owned by `SessionManager`, keyed by a renderer-issued `sessionId` (UUID) — NOT tied to a saved site id, so the same site can have multiple concurrent sessions (e.g. browsing + a tail), and the renderer's site-tabs feature keeps one `sessionId` alive per open tab.
- `TailManager`, `TerminalManager`, and `TransferQueue` are separate singletons that look up the `RemoteShell` for a given `sessionId` via `SessionManager.getInstance().shell(sessionId)` — they don't own connections themselves. `SessionManager.close(sessionId)` calls `stopAllForSession`/`closeAllForSession` on both `TailManager` and `TerminalManager` before ending the client — add the same hook here for any future per-session subsystem.

## Build / run / test

```bash
npm install --registry=https://registry.npmjs.org   # always pass --registry explicitly on this machine
npm run dev            # electron-vite dev (hot reload)
npm run typecheck       # vue-tsc + tsc, no emit — run this before every build
npm run build           # electron-vite build → out/
npm run package         # build + electron-builder → dist/*.exe (NSIS installer) + dist/win-unpacked/
```

`npm test` runs vitest (`vitest.config.ts`: Node environment, **`fileParallelism: false`** — see why below). Pure-logic unit tests: `shellEscape.test.ts`, `archive.test.ts`, `fileSort.test.ts`, `fileSelection.test.ts`, `KnownHostsStore.test.ts` (its `evaluateHostKey` decision function), `keyboardInteractive.test.ts` (`partitionPrompts`/`mergeAnswers`), `permissions.test.ts` (`parseMode`/`formatMode` alongside the pre-existing `toTechnical`/`toFriendlyLabel`) — none of these need a server. `RemoteShell.integration.test.ts`, `SessionManager.integration.test.ts` (host-key verification), `SessionManager.jumphost.integration.test.ts`, and `TransferQueue.integration.test.ts` are **real-server integration tests**: they connect an actual `ssh2.Client` (and, for the SessionManager/TransferQueue suites, go through the real `SessionManager`) to a local Docker SFTP/SSH test container and exercise real SFTP/SSH behavior — see the docker command in `RemoteShell.integration.test.ts`'s file header. Each integration file does an async top-level `probeServer()` before its `describe.skipIf(!serverAvailable)(...)` — if the container isn't running, the suite self-skips with a console warning instead of failing. Any file that touches `SessionManager`/`TransferQueue` (and transitively `TailManager`/`TerminalManager`, all real, unmocked) needs `vi.mock('electron', () => ({ BrowserWindow: { getAllWindows: () => [] } }))` at the top, since these singletons broadcast IPC events via `BrowserWindow.getAllWindows()` and vitest runs outside a real Electron process — follow this pattern (mock only `BrowserWindow`, nothing else) for any future integration test that touches a main-process singleton. **`fileParallelism: false` is load-bearing, not a style choice**: `SessionManager.integration.test.ts`, `SessionManager.jumphost.integration.test.ts`, and `TransferQueue.integration.test.ts` all legitimately touch the SAME real disk-backed `known_hosts.json` (via `KnownHostsStore`) for the SAME test-server host:port — running test *files* concurrently (vitest's default) raced on that shared state and produced a real, reproducible flaky failure before this was added; if you ever see `KnownHostsStore`-related test flakiness again, check this setting hasn't been reverted before debugging anything else.

## Conventions

- **IPC contract-first**: every new feature starts by adding channels/types to `src/shared/contract.ts`, then an `ipc/*.ipc.ts` handler file with `registerXHandlers()`, then wire it into `registerAllHandlers()` in `main/index.ts`.
- **fs/ services are plain exported functions, not classes** (stateless) — unlike `SessionManager`/`SiteStore`/`TailManager`/`TransferQueue`/`TerminalManager`, which ARE singletons (`getInstance()`) because they hold state.
- **Secrets never cross the IPC boundary decrypted.** `SiteStore` only returns `hasPassword`/`hasPassphrase` booleans to the renderer; decryption happens main-process-side, in-memory, at connect time only.
- **No silent auto-reconnect for browsing sessions** (stale listings/in-flight transfers would be unsafe) — only `TailManager` auto-reconnects, since re-tailing is safe.
- **Icons**: use `i-lucide-*` names anywhere (`UIcon`, `icon` props, even computed/dynamic bindings) — all Lucide icons are pre-registered offline, no need to worry about static-analysis detection.
- **Drag-and-drop has two distinct mechanisms, distinguished at the drop target by `event.dataTransfer.types.includes('Files')`.** Remote-row-to-local-pane (download) still uses a module-scoped Vue `ref` (`useDragAndDrop.ts`) carrying `{side, entry}` as in-app-only shared state (`dataTransfer` only carries `text/plain`, since there's no local file yet to hand the OS). Local rows instead call `system:startDrag` (new IPC, see below) on `dragstart`, which hands the real file path to `WebContents.startDrag` — this produces a native OS drag that (a) can be dropped onto Explorer/Finder or another app, and (b) is *also* what the remote pane's drop handler sees when the same drag lands back inside the app, alongside genuine OS-Explorer-file drops — both are treated identically as an upload (`FilePane.vue`'s `onOsDrop`). Never assume `getDragPayload()` is populated for a local-row drag anymore — check `isOsFileDrag(event)` first.
- **`ipc/envelope.ts` has a second registration helper, `handleWithEvent()`, alongside `handle()`.** Use it (instead of `handle()`) only when a handler needs the raw `IpcMainInvokeEvent` itself — today that's just `system:startDrag` (`event.sender.startDrag(...)`). Everything else should keep using plain `handle()`.
- **A component not yet emitted by `unplugin-vue-components`' scanner (i.e. missing from `src/renderer/components.d.ts`) is imported explicitly** via the package's public `./components/*` export path (e.g. `import ContextMenu, { type ContextMenuItem } from '@nuxt/ui/components/ContextMenu.vue'`) rather than relying on global `<UContextMenu>` registration + ambient types, which only exist for components some template has already used at the time `.d.ts` was generated. `vue-tsc --build` (used by `npm run typecheck`, which does NOT run Vite) never regenerates that file, so a genuinely new global-only usage would fail to type-check until the dev/build Vite pass runs once. `FileRow.vue`'s context menu is the first user of this pattern — follow it for the next not-yet-used Nuxt UI component instead of guessing at global registration.
- **Dialogs use `UModal`** (`v-model:open` or `:open` + `@update:open`, `#body`/`#footer="{ close }"` slots): `SiteFormDialog.vue`, `FilePreviewDialog.vue`, and the inline delete-confirm modal in `SessionManagerView.vue`. `UModal` also has an `#actions` slot rendered next to its built-in close button — used by `FilePreviewDialog.vue` for the tail-shortcut button instead of a fully custom `#header`. `USlideover` is reserved for contextual drill-down panels, not bounded CRUD forms — not used anywhere yet.
- **Toasts**: call `useNotify()` (`src/composables/useNotify.ts`), never `useToast()` directly — keeps styling (`color`, `icon`) consistent across call sites. Toast only on outcomes the user didn't just get obvious visual feedback for (transfer done/error, extract done/error, connect success/error, unexpected disconnect) — deliberately NOT on every fs op, site CRUD, or user-initiated disconnect; those already have an immediate visible UI change and toasting them would be noise.
- **Window chrome is fully custom** — `titleBarStyle: 'hidden'` alone (no `titleBarOverlay`, no `frame: false`), real Vue `<button>`s in `TitleBar.vue` wired to `window:minimize`/`window:maximizeToggle`/`window:close`/`window:isMaximized` IPC + a `window:state-change` broadcast event. Plain `<button>`, not `UButton`, for these three — `UButton`'s rounded/padded default styling doesn't match the flush, 44px-wide, square-hover-target Windows caption-button convention.
- **`main/index.ts` keeps a module-level `mainWindow` reference** (not just a local inside `createWindow()`) so IPC handlers (`window.ipc.ts`) and window event listeners (`maximize`/`unmaximize` → broadcast) can both reach the single app window.
- **Site tabs are a renderer-only concept.** `sessions.store.ts`'s state is `SessionTab[]` (`tabId`, `sessionId | null` while in "picker" state, `siteId | null` — set only for saved-site connects, used to dedup — `label`, `hostLabel` — `username@host`, shown by `TerminalView.vue` — `status`, `statusMessage`, `connecting`) + `activeTabId`; there is always ≥1 tab (closing the last one opens a fresh picker tab rather than leaving zero). `activeTab`/`activeSessionId`/`status`/`statusMessage`/`connecting` are **getters** that resolve from the active tab — this keeps `App.vue`/`FilePane.vue`/`SessionManagerView.vue` reading the exact same flat names they always did, with zero template changes. If you add a new such getter, never let anything assign to it directly (getters can't be assigned) — route mutations through an action that finds the specific tab object and mutates it (`tab.status = ...`), never `this.status`.
- **`connectToSite(site)` dedups by `siteId`.** Before opening a new connection it checks `tabs` for an existing tab with the same `siteId` and `status === 'connected' || connecting`; if found it just `setActiveTab`s to it (and cleans up the calling tab if that tab was an unused empty picker) instead of opening a duplicate session. Quick-connect (`connect()`, ad-hoc, `siteId: null`) is deliberately NOT deduped — there's no stable identity to dedup an ad-hoc host on. A previously **errored** tab for a site is also deliberately NOT matched (only `connected`/`connecting` are) — clicking Connect again opens a fresh tab rather than reusing/hiding the dead one, so the user can still see what failed.
- **The interactive Terminal now auto-opens in the background as soon as a site connects** (`sessions.store.ts`'s `openSession()` fires `terminalStreams.ensureTerminal(sessionId)` without awaiting, right after a successful connect) rather than waiting for the user to click the Terminal dock tab. Because of this, `terminalStreams.store.ts`'s `ensureTerminal()` must be safe against being called twice for the same session before the first call's `terminal:open` round-trip resolves (the auto-open racing a user's immediate click) — it tracks in-flight opens in a `pending: Map<string, Promise<string>>` alongside the resolved `instances` map, so a second overlapping call awaits the same promise instead of opening a second PTY. Keep this pattern if you add another eager/automatic call site for `ensureTerminal`.
- **`remoteFs.store.ts` is keyed by `sessionId`** (`bySession: Record<string, PerSessionFs>`) so each open site tab keeps its own independent `currentPath`/`entries`/`selected`/etc., but exposes the identical flat `currentPath`/`entries`/`loading`/`error`/`selected` getter surface as before (resolved via a `current` getter reading the active session's bucket, falling back to a frozen `EMPTY_DEFAULT` when no bucket exists yet) — `FilePane.vue` needed no template changes for this either. Lazy-create a session's bucket only inside actions (e.g. `ensureBucket()` at the top of `load()`), never inside a getter (Pinia/Vue getters must stay pure — no side effects). There's a real (intentional, safe) circular import between `sessions.store.ts` and `remoteFs.store.ts`/`terminalStreams.store.ts` (each calls the other's `useXStore()` inside action bodies, never at module-eval time) — this is a standard, working ESM circular-import pattern for cross-referencing Pinia stores; don't "fix" it by breaking the reference apart.
- **Live xterm.js `Terminal` instances live in a plain module-scoped `Map` inside `terminalStreams.store.ts`, deliberately OUTSIDE Pinia's reactive state**, keyed by `sessionId` (one terminal per session). A single `terminal:data` IPC subscription (set up once, lazily) routes each chunk to the right cached instance — components never subscribe individually. `TerminalView.vue` only ever attaches/shows the DOM (`term.open(el)` exactly once ever per instance, guarded by a local `Set`); it never constructs or disposes a `Terminal` itself. This is the pattern to follow for any future feature that needs a stateful, non-serializable client object (a socket, a canvas context, a third-party widget) to survive across tab/dock-panel switching without being destroyed by Vue's `v-if`.
- **A dock tab that must survive `BottomDock.vue`'s own tab-switching (and its collapse toggle) uses `v-show`, not `v-if`/`v-else-if`.** Transfers/Log Tail are stateless-enough to freely remount (the existing `v-if`/`v-else-if` chain), but `TerminalView` sits in its own always-present sibling `<div v-show="!collapsed && tab === 'terminal'">` (mounted once, guarded by a `terminalEverShown` flag) specifically so switching dock tabs or collapsing the dock never tears down its live xterm instances.
- **The Local-pane hide toggle lives in `SiteTabBar.vue`, pinned to the right end** (a `<div class="flex-1">` spacer separates it from the tab chips + "+" button), not in `FilePane.vue`'s own header — but the collapse/expand logic itself (`collapsedRail`/`showBody` computed pair, the fixed `h-8` header row on both sides, the watcher that closes any open preview dialog on collapse) still lives entirely in `FilePane.vue`/`ui.store.ts`; only the trigger button's location moved. Hiding the Local pane still collapses it to a slim rail, it never fully unmounts.
- **Icon-only buttons get wrapped in `<UTooltip text="...">`, everywhere** (reusing existing `aria-label` text where present). `UTooltip`'s default slot renders its trigger via `asChild` (reka-ui) — no extra wrapper DOM node is added, so it's safe to wrap hover-reveal buttons (`class="opacity-0 group-hover:opacity-100"`) without breaking that pattern. Buttons with a visible text label (e.g. `BottomDock.vue`'s Transfers/Log Tail/Terminal tab buttons) don't need one.
- **`FileRow.vue`'s per-row action icons (tail/extract/transfer/delete) each sit in their own fixed-width `w-7` slot**, not bare conditionally-rendered buttons — because which icons apply varies per row (a directory shows none of tail/extract/transfer, only delete; a non-zip file skips extract), a bare `v-if` would let the row's flex layout collapse and shift later icons (most visibly, `delete`) leftward depending on how many earlier icons applied. Wrapping each action in `<div class="flex w-7 shrink-0 items-center justify-center">` (with the actual button/tooltip `v-if`'d inside) reserves the slot's width even when empty, so every row's icons land in identically-positioned columns. The same fixed-width-column technique is used for the remote-only permissions cell (`w-36`, with an equal-width empty placeholder div when a row's `side !== 'remote'`) — follow this pattern for any future per-row column that isn't always present/isn't fixed-length text.
- **Remote-only file permissions**: `FileEntry.permissions` (a 4-digit octal string, e.g. `"0755"`) is populated only by `RemoteFsService`/`RemoteShell.ts` — `LocalFsService.ts` never sets it, since local (Windows) `fs.stat().mode` isn't meaningful POSIX data. `renderer/src/utils/permissions.ts` formats it two ways: `toTechnical()` (classic `rwxr-xr-x`, all three owner/group/other triplets) and `toFriendlyLabel()` (a single compact phrase derived from the OWNER triplet only, e.g. "Read & Write" — explicitly scoped to the owner, not the connected user's own effective access, since those can differ on a shared server; the full technical string is always one hover away via the cell's tooltip). `ui.store.ts`'s `permissionsDisplay` (persisted, default `'friendly'`) picks which one `FileRow.vue` renders; toggled via `FileToolbar.vue`'s permissions-toggle button (remote pane only, gated by its own `side` prop).
- **Custom scrollbar styling reuses `@nuxt/ui`'s own runtime CSS vars** (`--ui-border-accented`, `--ui-color-neutral-400`/`500`) in `main.css`'s global `::-webkit-scrollbar*` rules, rather than hardcoding new colors — keeps the scrollbar in sync with the app's theme automatically, the same way the file's pre-existing `--ui-primary` override already does.
- **Deferred-but-still-full icon-set loading, and lazy-loaded post-connect components, keep cold start off the JS-parse critical path.** `main.ts` no longer statically imports `@iconify-json/lucide/icons.json` (a static import is hoisted and parsed before any other top-level code runs, blocking first paint, regardless of where the `addCollection()` *call* sits in the file) — it's a dynamic `import()` scheduled via `requestIdleCallback`/`setTimeout` fallback, run *after* `mount()`. It still registers the WHOLE set (not a hand-picked subset) because some `i-lucide-*` names are bound dynamically (computed props, ternaries) and Vite's static icon-bundling can't detect those — icons requested before the deferred load resolves just pop in a frame later, which Iconify's runtime handles natively. Same pattern for `@xterm/xterm`/`@xterm/addon-fit`: type-only imports at the top of `terminalStreams.store.ts`, the real classes dynamically imported inside `ensureTerminal()` right before construction. And `App.vue`'s `FilePane`/`BottomDock` (only ever rendered post-connect) are `defineAsyncComponent(() => import(...))` rather than static imports — `TitleBar`/`SiteTabBar`/`SessionManagerView` stay static since they render at first paint. If you add another library/component that's expensive to parse but not needed until some later user action, follow this same "type-only import + dynamic import at the point of use" shape rather than a static import.
- **Shell-command construction shares one escaping helper.** `src/main/ssh/shellEscape.ts` (single-quote POSIX escaping, embedded `'` → `'\''`) is imported by both `UnzipService.ts` (building `unzip`/`tar` commands) and `TerminalManager.ts` (injecting a `cd` on terminal open) — don't reintroduce a local private copy in a new call site; import this one.
- **Archive-extension recognition lives in `src/shared/archive.ts`, not hardcoded per call site.** `archiveKind()`/`isArchive()` are pure functions (no Electron/Node deps) imported by both the renderer (`fileTypes.ts`, re-exported for `FileRow.vue`'s "Extract here" gating) and the main process (`UnzipService.ts`, choosing `unzip` vs. `tar -xf`/`-xzf`/`-xjf` per kind) — this is the shared-module pattern to follow for any future renderer/main-agreed classification logic (mirrors how `contract.ts` itself is shared, but scoped to domain logic rather than IPC shapes). `.7z`/`.rar` are deliberately unrecognized (not reliably available on remote hosts) — don't add them without also designing new missing-tool error handling.
- **The Terminal lands in the site's configured remote start path, not the SSH default.** SSH's shell-channel protocol has no cwd concept at all (confirmed against `@types/ssh2`) — `TerminalManager.open()` writes a `cd <shellEscape(path)>\n` into the freshly-opened stream (after listeners are attached, so the shell's response is forwarded like any typed command) when `SessionManager.cwd(sessionId)` resolves to something other than `.`. That value is already seeded from `Site.remoteInitialPath` at connect time for the file browser's benefit — this just added a second consumer, no new plumbing. If you add another feature that needs a session's configured start path, read `SessionManager.cwd(sessionId)`, don't add new IPC plumbing for it.
- **One shared `.fade-*` CSS transition (`main.css`) covers every simple show/hide in the app** — the title-bar busy dot and `App.vue`'s picker/connected swap both use `<Transition name="fade">` with these classes rather than each defining their own. Reuse it for future similar cases instead of writing a new one-off transition.
- **Do not manufacture a transition by re-keying/re-mounting `FilePane`/`BottomDock`/`TerminalView` per active site tab.** Switching between two already-connected tabs deliberately does NOT swap those components — same instances, different session data via computed getters (see the `remoteFs.store.ts`/`terminalStreams.store.ts` entries above) — forcing a remount there to animate the switch would tear down live xterm scrollback and cached fs listings that this architecture specifically preserves. The only top-level state boundary that gets a fade is the picker↔connected swap in `App.vue`.
- **CSS transitions on a collapse/expand toggle need explicit sizes on BOTH states — a `flex-1`/no-explicit-value side will not animate.** `FilePane.vue`'s Local-pane rail collapse animates `width` between `w-10` (collapsed) and `min-w-0 w-1/2` (expanded, LOCAL side only — the ternary is conditioned on `side === 'local'`, since `FilePane.vue` is instantiated twice from one template and the Remote pane must keep its own unrelated `flex-1` untouched in both of Local's states, or it won't grow to fill the space Local's rail frees up). `min-w-0 flex-1` (flex-grow-derived, no explicit `width`, computed style stays `width: auto`) was tried first and produced NO visible animation at all — CSS cannot interpolate to/from an `auto`-resolved width, only between two concrete `<length-percentage>` values (`BottomDock.vue`'s dock collapse works for the same reason: `h-9`/`h-56` are both explicit). If a future pane/panel collapse needs the same treatment, give both states an explicit length — never leave one side as bare `flex-1`/`auto`.
- **A generic in-flight IPC-call counter (`ipcActivity.store.ts`), not hand-picked per-action flags, drives the title-bar busy indicator.** `api.ts`'s `invoke()` increments/decrements this store's `count` around every call (skipping a small `QUIET_CHANNELS` exclusion set: `terminal:write`/`terminal:resize` — fire per keystroke/resize — and the four window-chrome channels, semantically unrelated to "SSH/SFTP work"). `useGlobalActivity.ts`'s `isBusy` is primarily this counter (`ipcActivity.isBusy`), so any new IPC channel is automatically covered without remembering to wire it in — the composable's `connecting`/transfer-count/fs-`loading` checks exist only to pick a nicer LABEL when they're the active reason, falling back to a generic "Working…" otherwise. If you add a new high-frequency channel (fires on a timer or per-keystroke/per-frame), add it to `QUIET_CHANNELS`, don't leave it tracked.
- **A composable that returns multiple refs (e.g. `useGlobalActivity()`'s `{ isBusy, label }`) must be destructured at the call site, not accessed via the returned object's dot-notation, if used directly in a template.** `const activity = useGlobalActivity(); ...{{ activity.isBusy }}...` does NOT auto-unwrap — `activity` is a plain object, and Vue's template compiler only auto-unwraps refs that are top-level `<script setup>` bindings (or accessed through a `reactive()`-wrapped object), not nested properties reached via dot-access on a plain object. Always write `const { isBusy, label } = useGlobalActivity()` and reference `isBusy`/`label` directly in the template. Getting this wrong doesn't error at build time — it silently passes the raw `Ref` object as a prop/condition (a `v-if` on a raw ref object is always truthy, and a `:prop="someRef"` binding throws a runtime "invalid prop type" warning), so watch for that failure shape if a composable's boolean/value never seems to update in a template.
- **Recursive directory transfer is one `TransferQueue` job, not N.** `enqueueTree()` pushes a `TransferJob` with `isTree: true` into the SAME `pending`/`active` queue as single-file jobs (so the existing `MAX_CONCURRENT`/cancel machinery needs no changes), but `runTree()` then walks the whole source tree (`LocalFsService.listRecursive`/`RemoteShell.readdirRecursive`), recreates every directory at the destination (`mkdir -p`-equivalent, tolerant of already-existing dirs), and transfers its files **sequentially, one at a time** — not fanned out to the pool. `runFile()` (the byte-streaming core, shared by both a solo file job and each file inside a tree job) takes an `onProgress` callback rather than broadcasting directly, so `runTree()` can aggregate all its files' bytes into ONE throttled `progress` event under the job's single `transferId` — the renderer never learns a tree transfer contains more than one file. If you need per-tree concurrency (multiple files of the same tree in flight at once) that's a deliberate follow-up, not an oversight — sequential was chosen to keep this job type simple and to not blow past `MAX_CONCURRENT` real SFTP streams when several tree jobs happen to be active together.
- **File-list sort and selection logic are pure functions, not inlined into the stores.** `renderer/src/utils/fileSort.ts`'s `compareEntries(column, direction)` and `fileSelection.ts`'s `selectOnly`/`toggleSelect`/`selectRange`/`selectAll` are imported identically by both `localFs.store.ts` (flat state) and `remoteFs.store.ts` (per-session `PerSessionFs` bucket) — each store just assigns the pure function's result onto its own state shape. `selectRange` needs a `selectAnchor` (the last plain-click or ctrl-click target) alongside `selected`; both stores carry it, reset to `null` on every fresh `load()`. Keep new sort/selection behavior in these shared modules, not duplicated per store, if a third fs-like store is ever added.
- **Host-key verification is trust-on-first-use (TOFU), pinned per `host:port` in `KnownHostsStore` (`known_hosts.json`, electron-store-backed).** `SessionManager.buildConnectConfig()` builds a `HostVerifier` (ssh2's `ConnectConfig.hostVerifier`) around the pure decision function `evaluateHostKey(knownFingerprint, presentedFingerprint, forceTrust)` in `KnownHostsStore.ts` — `'trust-new'` (nothing on file yet, or the caller force-trusts a change) writes the fingerprint and calls `verify(true)`; `'match'` calls `verify(true)` without writing; `'mismatch'` calls `verify(false)`, and the surrounding `connect()` catch block turns that into a `HOST_KEY_MISMATCH` `SshError` naming both fingerprints. Never call `verify(false)` without having stashed the expected/presented pair somewhere the catch block can read — the raw ssh2 error on a `hostVerifier` rejection is a generic, unhelpful "connection failed" otherwise.
- **A `let` reassigned only inside a closure passed to another function gets incorrectly narrowed by TypeScript at read sites *outside* that closure — use a mutable holder object (`{ value: T | null }`) instead of a bare nullable `let` whenever a callback (not straight-line code) is what sets it.** Hit this for real building the host-key-mismatch plumbing: `let hostKeyMismatch: X | null = null` reassigned only inside the `hostVerifier` callback, then read in the `catch` block via `hostKeyMismatch ? ... : ...`, type-checked to `never` in the truthy branch — TypeScript's control-flow analysis doesn't track assignments inside nested functions, so it treated the read site as if the variable could only ever be `null`. `SessionManager.ts`'s `HostKeyMismatchHolder` interface is the fix; reach for the same pattern (not a type-assertion workaround) for any future "a callback sets this, code after the callback reads it" case.
- **`SessionManager.connect()`'s real 2FA/OTP flow only forwards prompts it can't answer itself — it does NOT forward every keyboard-interactive challenge to the user.** `keyboardInteractive.ts`'s pure `partitionPrompts(prompts, password)` auto-answers anything matching `/password/i` (many servers route plain password auth through keyboard-interactive under PAM — confirmed for real against the test container) and only escalates the rest via `promptKeyboardInteractive()` (broadcasts `session:keyboard-interactive-prompt`, resolves when `session:keyboard-interactive-respond` arrives with a matching `requestId`, tracked in `SessionManager`'s `pendingKeyboardInteractive` map). `mergeAnswers()` recombines both halves back into ssh2's expected answer-array order. If you need to add auto-answer heuristics for another common prompt shape, extend `partitionPrompts` — don't special-case it inline in `SessionManager.connect()`.
- **Jump-host tunneling reuses the exact same per-hop connect machinery as the main target, called twice.** `SessionManager.connect()` calls `buildConnectConfig()`/`connectClient()` once for the jump host (if `input.jumpHost` is set) and once for the real target — each hop gets its own `HostVerifier`/`KnownHostsStore` entry (keyed by its own `host:port`), and real keyboard-interactive prompts from EITHER hop route through the same `sessionId` (harmless — it's just informational context for the renderer dialog, not a routing key; routing is by `requestId`). The target's `ConnectConfig.sock` is set to the `ClientChannel` returned by `jumpClient.forwardOut()` (`forwardThroughJump()`) — ssh2 then tunnels the whole second SSH handshake through that stream instead of opening its own TCP socket. `SessionEntry.jumpClient` is ended alongside the main `client` in `close()` and on any connect failure — a jump-host session that never cleans up its bastion hop would leak a connection on every failed/closed session.
- **`defaultAgentPath()` (`SessionManager.ts`) is the only place platform-specific ssh-agent resolution lives — don't inline `process.platform` checks elsewhere for this.** Windows defaults to the modern OpenSSH Agent's named pipe (`\\.\pipe\openssh-ssh-agent`); non-Windows defaults to `$SSH_AUTH_SOCK`, throwing `VALIDATION` if unset. A site's `agentPath` override (surfaced in `SiteFormDialog.vue`) takes precedence over this default entirely — including the literal string `pageant`, which `ssh2`'s own `createAgent()` special-cases to talk to PuTTY's Pageant instead of a named pipe/socket. This path has NOT been verified against a real running agent in this environment (no agent was available) — unlike jump-host, which was verified via a self-jump trick against the real test container.
- **`ChmodDialog.vue`'s 3×3 checkbox grid is written as explicit owner/group/other `<tr>`s, not a `v-for` over an object keyed by triplet name.** A `v-for="(label, key) in {owner:'Owner',...}"` + `tripletsObj[key]` pattern was considered and rejected — indexing a reactive object by a `v-for`-widened key fails `vue-tsc`'s strict template type-checking (the inferred key type is a plain `string`, not a literal union). Three explicit rows is more code but the only version that actually type-checks cleanly; follow this if another fixed-cardinality grid ever needs the same shape.
- **`permissions.ts`'s `parseMode`/`formatMode` are the ONLY sanctioned octal-string <-> triplet conversion — `toTechnical`/`toFriendlyLabel`'s internal `parseTriplet` stayed private on purpose.** `ChmodDialog.vue` needs round-trip (parse for the initial checkbox state, format back to a mode string to submit) where the read-only display helpers only ever needed one direction. If a future feature needs the triplets themselves (not just a formatted string), import `parseMode`/`Triplet`/`ModeTriplets` from `permissions.ts` rather than re-deriving bitmasks inline.
- **Transfer retry re-enqueues from scratch under a NEW `transferId` — it does not resume or reuse the failed job.** `transferQueue.store.ts`'s `retry()` deletes the old (`error`/`cancelled`) entry from its `items` map and calls `this.enqueue()` again with the same `sessionId`/`kind`/paths/`isDir` it cached on the original entry — this is why `TransferItemState` grew `sessionId`/`isDir` fields (previously only implicit in the `enqueue()` call's arguments, never persisted). Nothing in `TransferQueue.ts` (main process) knows about "retry" — it's purely a renderer-side "do the same enqueue call again" convenience, which is also why it needed no new IPC channel and no new real-server test (the underlying `enqueue()`/`enqueueTree()` path is already covered).

## Gotchas that will waste your first hour

1. **`electron-builder.yml` must be named EXACTLY that** (or `electron-builder.ts`/`.json`/etc.) — NOT `electron-builder.config.ts`. electron-builder's config loader (`read-config-file`) only searches for `electron-builder.<ext>`; `*.config.ts` is silently ignored with no error, and you get default settings (`oneClick: true`, wrong product name, no asarUnpack). We hit this for real — see `.claude/plan/ferry-implementation-plan.md` history. Also: electron-builder's `.ts`-config loader (`config-file-ts`) has a Windows bug where absolute paths with a drive letter break its cache-directory naming — prefer `.yml` over `.ts` for this config on Windows regardless.
2. **Icons will silently 404/CSP-block unless the full Lucide set is registered — AND the network-fallback path is disabled, not just deferred.** `@nuxt/ui`'s icon component falls back to fetching unrecognized icon names from Iconify's public API at runtime — blocked by our CSP (`default-src 'self'`), and Vite's static icon-bundling can't detect dynamically-bound icon names (computed props, ternaries) anyway. `main.ts` registers the full Lucide set via a dynamic `import()` deferred to `requestIdleCallback` (so cold-start isn't blocked on parsing 554KB of JSON) — but that alone isn't sufficient, since icons rendered in the first frame (before the deferred load resolves) would still hit the network-fallback path and get CSP-blocked. `main.ts` also calls `setCustomIconLoader(loader, 'lucide')` (from `@iconify/vue`) with a loader that `await`s the SAME memoized load-then-`addCollection()` promise used by the idle-scheduled call, returning `null` once it resolves — this is NOT a no-op/never-resolving loader (a loader that never settles would permanently strand any icon that hit it before the real load finished, since Iconify only re-notifies already-mounted `Icon` instances when a load attempt actually *settles*, not merely when `addCollection()` is later called on its own). If you add a different icon collection, register both halves (the deferred `addCollection` load AND a `setCustomIconLoader` gating on that same promise) the same way — don't just add a bare `addCollection()` call.
3. **`titleBarStyle: 'hidden'` alone removes ALL window chrome on Windows** — no close/minimize/maximize button, only Alt+F4 — and that's now intentional: `TitleBar.vue` draws its own buttons wired to `window:*` IPC (see Conventions). Do NOT re-add `titleBarOverlay` "to be safe" — it draws real OS caption buttons over the custom bar with no way to hide just the buttons while keeping the overlay, which is exactly what we removed it to avoid.
4. **`tsconfig.web.json`'s `include` must cover `src/renderer/auto-imports.d.ts`, not just `src/renderer/src/**/*`.** `unplugin-auto-import` (via `@nuxt/ui/vite`) writes global ambient declarations (`useToast`, `useOverlay`, etc.) to that file, one directory above the renderer source root. Component tags (`UButton`, `UModal`, ...) still resolve fine in `<template>` even without it, because Vue's template checker (`@vue/language-core`) discovers `components.d.ts`-style files through a separate mechanism — but a bare composable call in `<script setup>` (e.g. `useToast()`) goes through the real TS program and needs the ambient file in `include` to type-check. Hit this for real adding `useNotify.ts` — fixed by adding the path explicitly.
5. **OS drag-and-drop (`system:startDrag`, reading `File.path` off dropped items) depends on an Electron API surface that changes at v32.** This project is pinned to Electron 30.5.1, where the legacy `File.path` extension (used in `FilePane.vue`'s `onOsDrop` and implicitly by `WebContents.startDrag`'s renderer-side trigger) still works under `sandbox: true`. Electron 32 introduces `webUtils.getPathForFile()` as the sandboxed-safe replacement and begins deprecating `File.path`; upgrading Electron past 32 will need `onOsDrop` migrated to call `webUtils.getPathForFile(file)` via a small preload-exposed helper instead of reading `.path` directly off the DOM `File` object. Don't upgrade Electron without checking this first — OS drag-and-drop would silently stop working (no path, no upload) rather than erroring loudly.
6. **App icon is now set** — `resources/1024x1024.png` is electron-builder's `win.icon` source (auto-generates every Windows resolution from one square PNG), `resources/favicon.ico` is the `BrowserWindow`'s dev-mode-only icon (`main/index.ts`, gated on `IS_DEV` since packaged builds inherit the exe's embedded icon instead), and `src/renderer/public/favicon.ico` (referenced via a relative `<link rel="icon">` in `index.html`) covers the (mostly invisible, given the custom titlebar) renderer-side favicon.
7. `@toys\ui` (the local `@nuxt/ui` source clone) is NOT a dependency of this project and has no build output — it's reference material only (source browsing + the `nuxt-ui` skill). Never point tooling at it.
8. **File preview/download/tail affordances only apply to the remote pane, never local.** `FilePreviewDialog.vue`'s download and tail buttons are gated on `side === 'remote'` — downloading or tailing a file that's already on the local machine doesn't make sense, and `TailManager`/`tail:start` only ever operates over an SSH session. If you add a similar affordance elsewhere, gate it the same way (mirrors `FilePane.vue`'s pre-existing `showTail = side === 'remote' && connected` computed).
9. **Never call `xterm.js`'s `term.open()` more than once per `Terminal` instance.** Reparenting a live terminal onto a second element isn't a documented-safe operation, so `TerminalView.vue` guards it with a `Set` of already-attached session ids and relies on the container staying permanently in the DOM (`v-show`, see Conventions) instead. If a future refactor needs the container to actually unmount/remount, dispose the old `Terminal` and create a fresh one rather than re-opening it.
10. **Terminal output crosses IPC as raw `Uint8Array`, not a decoded string.** `TerminalManager` forwards each `stream.on('data', chunk: Buffer)` as `new Uint8Array(chunk)` rather than `chunk.toString()`, because a naive per-chunk UTF-8 decode can split a multi-byte character across two chunks and corrupt it; `xterm.js`'s `Terminal.write()` accepts `Uint8Array` directly so no decode step is needed in the renderer either. Keystrokes going the other way (`terminal:write`) are a plain string — that direction has no chunk-boundary problem.

## Where to go for X

| Task | Files |
|---|---|
| Add a new IPC channel | `shared/contract.ts` → new `ipc/*.ipc.ts` handler → `main/index.ts` `registerAllHandlers()` → renderer store/composable calling `invoke()`/`onEvent()` |
| Change SSH/SFTP behavior | `main/ssh/RemoteShell.ts` (low-level ops), `main/ssh/SessionManager.ts` (connection lifecycle) |
| Change saved-site management UI | `renderer/src/components/sessions/SessionManagerView.vue` (list), `SiteFormDialog.vue` (add/edit form), `renderer/src/stores/sites.store.ts` (hostname-fallback logic lives here) |
| Change the file preview dialog / text-preview or log-file extension lists | `renderer/src/components/files/FilePreviewDialog.vue`, `renderer/src/utils/fileTypes.ts` |
| Change the download default location | `main/ipc/system.ipc.ts` (`system:getDownloadsPath`), consumed in `renderer/src/components/files/FilePane.vue`'s `transferEntry` |
| Add/change a toast notification | `renderer/src/composables/useNotify.ts`; call sites today: `stores/transferQueue.store.ts`, `stores/sessions.store.ts`, `components/files/FilePane.vue`'s `onExtract` |
| Change window control behavior (minimize/maximize/close) | `main/ipc/window.ipc.ts`, `renderer/src/components/shell/TitleBar.vue` |
| Change the tail command / rotation handling | `main/tail/TailManager.ts` (`follow()` method, the `tail -F` command string) |
| Change transfer concurrency/progress throttling | `main/transfer/TransferQueue.ts` (`MAX_CONCURRENT`, `PROGRESS_THROTTLE_MS`) |
| Change visual theme/tokens | `renderer/src/assets/main.css` (`@theme` blocks, `--ui-*` CSS vars) |
| Add a new dock tab (beyond Transfers/Log Tail/Terminal) | `renderer/src/components/shell/BottomDock.vue` (`DockTab` union + button + template branch — use `v-show` instead of `v-if` if the tab holds state that must survive switching, see Conventions) |
| Change the site-tabs / multi-session model | `renderer/src/stores/sessions.store.ts` (`SessionTab[]`, back-compat getters, `openNewTab`/`setActiveTab`/`closeTab`), `renderer/src/stores/remoteFs.store.ts` (per-session `bySession` keying), `renderer/src/components/shell/SiteTabBar.vue` |
| Add/change the Terminal feature | `main/terminal/TerminalManager.ts`, `main/ssh/RemoteShell.ts` (`openShell`), `main/ipc/terminal.ipc.ts`, `renderer/src/stores/terminalStreams.store.ts` (xterm.js instance cache), `renderer/src/components/terminal/TerminalView.vue`, `renderer/src/components/shell/BottomDock.vue` |
| Change the file-permissions column/display | `renderer/src/utils/permissions.ts` (formatting), `renderer/src/stores/ui.store.ts` (`permissionsDisplay` setting), `renderer/src/components/files/FileRow.vue` (cell), `FileToolbar.vue` (toggle button) |
| Add a keyboard shortcut scoped to a file pane | `renderer/src/components/files/FilePane.vue`'s `onKeydown` (the only shortcut-registration pattern in the renderer today — per-pane `tabindex="0"` + keydown, not a global listener) |
| Packaging/installer settings | `electron-builder.yml` (see Gotcha #1 about the filename) |
| Change which file extensions preview as text, or archive-extraction support | `renderer/src/utils/fileTypes.ts` (`TEXT_EXTENSIONS`), `shared/archive.ts` (`archiveKind`/`isArchive`), `main/unzip/UnzipService.ts` (per-kind command builder) |
| Change the global "something is processing" indicator | `renderer/src/stores/ipcActivity.store.ts` (the generic in-flight counter, incremented/decremented by `api.ts`'s `invoke()`), `renderer/src/composables/useGlobalActivity.ts` (label-priority logic), `renderer/src/components/shell/TitleBar.vue` (the dot) |
| Change the displayed app version | `main/ipc/system.ipc.ts` (`systemGetAppVersion` handler, reads `app.getVersion()`), `renderer/src/components/sessions/SessionManagerView.vue` (footer text) |
| Change cold-start bundle-eval work (icon set, xterm, post-connect components) | `renderer/src/main.ts` (Lucide deferral), `renderer/src/stores/terminalStreams.store.ts` (`@xterm/*` dynamic import), `renderer/src/App.vue` (`defineAsyncComponent` for `FilePane`/`BottomDock`) |
| Change what path a fresh Terminal shell lands in | `main/terminal/TerminalManager.ts`'s `open()` (the `cd` injection), backed by `SessionManager.cwd(sessionId)` (already seeded from `Site.remoteInitialPath`) |
| Change recursive directory transfer | `main/transfer/TransferQueue.ts` (`runTree`/`runFile`), `main/fs/LocalFsService.ts` (`listRecursive`/`mkdirRecursive`), `main/ssh/RemoteShell.ts` (`readdirRecursive`/`mkdirRecursive`) |
| Change file-list sort or multi-select behavior | `renderer/src/utils/fileSort.ts`, `renderer/src/utils/fileSelection.ts`, `renderer/src/stores/localFs.store.ts`/`remoteFs.store.ts` (thin wrappers around both) |
| Add/change a right-click context-menu action | `renderer/src/components/files/FileRow.vue`'s `contextMenuItems` computed (note the `@nuxt/ui/components/ContextMenu.vue` deep-import pattern, see Conventions) |
| Change rename behavior | `renderer/src/components/files/FileRow.vue` (inline input + F2), `FilePane.vue` (`renamingPath`, F2 keybinding, context-menu `start-rename`), `stores/localFs.store.ts`/`remoteFs.store.ts` (`rename` action) |
| Change OS drag-and-drop behavior | `main/ipc/system.ipc.ts` (`systemStartDrag`), `renderer/src/components/files/FileRow.vue` (`onDragStart`'s local-row native-drag handoff), `FilePane.vue` (`isOsFileDrag`/`onOsDrop`) — see Gotcha #5 before touching this on a newer Electron |
| Change host-key verification / TOFU behavior | `main/ssh/KnownHostsStore.ts` (`evaluateHostKey`, `fingerprintHostKey`, persistence), `main/ssh/SessionManager.ts` (`buildConnectConfig`'s `hostVerifier`), `renderer/src/stores/sessions.store.ts` (`pendingHostKeyMismatch`, `acceptHostKeyAndRetry`), `SessionManagerView.vue` (warning dialog) |
| Change the real keyboard-interactive/2FA prompt flow | `main/ssh/keyboardInteractive.ts` (`partitionPrompts`/`mergeAnswers`), `main/ssh/SessionManager.ts` (`connectClient`, `promptKeyboardInteractive`, `respondKeyboardInteractive`), `renderer/src/stores/sessions.store.ts` (`pendingKeyboardPrompt`), `SessionManagerView.vue` (prompt dialog) |
| Change SSH-agent auth or jump-host/proxy support | `main/ssh/SessionManager.ts` (`defaultAgentPath`, `buildConnectConfig`, `connectClient`, `forwardThroughJump`), `shared/contract.ts` (`AuthMethod`, `JumpHostConfig`/`JumpHostInfo`), `main/sites/SiteStore.ts` (encrypted jump-host secrets), `SiteFormDialog.vue` (agent-path override + jump-host sub-form) |
| Change chmod/editable permissions | `main/ssh/RemoteShell.ts` (`chmod`), `main/fs/RemoteFsService.ts` (`chmodRemote`), `renderer/src/components/files/ChmodDialog.vue`, `renderer/src/utils/permissions.ts` (`parseMode`/`formatMode`), `stores/remoteFs.store.ts` (`chmod` action) |
| Change transfer retry behavior | `renderer/src/stores/transferQueue.store.ts` (`retry`), `components/transfers/TransferItem.vue` (retry button, `retryable` gating) — purely renderer-side, reuses `enqueue()`/`enqueueTree()` |
